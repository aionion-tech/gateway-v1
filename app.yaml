runtime: nodejs18
env: flex
build_env_variables:
  GOOGLE_NODE_RUN_SCRIPTS: ""
env_variables:
  NODE_ENV: "production"
  AUTH_SERVICE_URL: $AUTH_SERVICE_URL
  PORT: $PORT
automatic_scaling:
  max_instances: 2
